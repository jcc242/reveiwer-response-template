# reveiwer-response-template
A LaTeX template for responding to peer review comments.

Use the included Makefile to compile the document with GNU Make by running ```make``` in this project's directory.
